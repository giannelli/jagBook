<?xml version="1.0" encoding="UTF-8" ?>
<chapter xml:id="basics">
    <title>Basic Sage Commands</title>

    <introduction>
        <p>Here you will find a hotch potch of some basic SageMath concepts</p>
    </introduction>

    <section>
        <title>How to define functions</title>

        <sage>
            <input>
                def f(z): return 2*z^3 - z^2 - 7*z + 2
                type(f)
            </input>
            <output>
                &lt;type 'function'&gt;
            </output>
        </sage>

        <sage>
            <input>
                f(2)
            </input>
            <output>
                0
            </output>
        </sage>

        <sage>
            <input>
                plot(f, -2, 3)
            </input>
        </sage>

        <sage>
            <input>
                f(x) = (cos(x)-1)/x^2
                show(f)
            </input>
        </sage>
        <sage>
            <input>
                f(3)
            </input>
            <output>
                1/9*cos(3) - 1/9
            </output>
        </sage>
        <sage>
            <input>
                show(f(pi))
            </input>
            <output>
                -2/pi^2
            </output>
        </sage>
        <sage>
            <input>
                plot(f(x), (-2*pi, 2*pi),ymin = -1, ymax = 1)
            </input>
        </sage>
    </section>

    <section>
        <title>Symbolic Expressions</title>

        <p>The expand method</p>
        <sage>
            <input>
                z = (x+1)^3
                show(z)
            </input>
            <output>(x+1)^3</output>
        </sage>

        <sage>
            <input>
                z2 = z.expand()
                show(z2)
            </input>
            <output>x^3 + 3*x^2 + 3*x + 1</output>
        </sage>
        <p>Notice how the <code>show()</code> method displays the output in a nicer manner.  Without it you will see the less pretty output: <pre>x^3 + 3*x^2 + 3*x + 1</pre> </p>

        <p>Now the reverse!  Perform a <code>factor()</code> on the expanded equation.</p>
        <sage>
            <input>
                show(z2.factor())
            </input>
            <output>x^3 + 3*x^2 + 3*x + 1</output>
        </sage>
    </section>

    <section>
        <title>Declaring Variables</title>

        <subsection>
            <title>Use SageMath to solve differential equations</title>

            <p>Consider the equation <m>\frac{dy}{dx} and \frac{d^2 u}{dx^2}y</m></p>

        </subsection>
        <sage>
            <input>
                z = (x+1)^3
                show(z)
            </input>
            <output>(x+1)^3</output>
        </sage>

    </section>

</chapter>
